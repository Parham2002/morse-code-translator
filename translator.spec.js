import { translateToMorse } from "./translator";

describe('Testing translateToMorse', () => {
  it("2 should be 2", ()=> {
    // Arrange
      let number;
    // Act
      number = 5;
    // Assert
    expect(number).toBe(2)
  })

 })